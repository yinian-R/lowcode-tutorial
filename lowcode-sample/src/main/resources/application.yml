server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/lowcode_sample?characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&useUnicode=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging:
  level:
    root: info
    com.example.lowcodesample: debug
